#!/bin/bash
## Jenil_Patel_202101074
## count all files in given path 
path_=$1
if [[ ! $1 ]]; then
    path_=$(pwd)
fi

# find ${path_} -type f | wc -l
# ls -R ${path_} | wc -l
ls -R ${path_}

# --files0-from=F
